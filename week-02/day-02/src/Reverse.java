import java.util.ArrayList;

public class Reverse {
  public static void main(String[] args) {
    String toBeReversed =
        ".eslaf eb t'ndluow ecnetnes siht ,dehctiws erew eslaf dna eurt fo sgninaem eht fI";
    // Create a method that can reverse a String, which is passed as the parameter
    // Use it on this reversed string to check it!
    // Try to solve this using charAt() first, and optionally anything else afte//    int i = toBeReversed.length();                   //determining the length of the string/    while (i > 0) {{
//      toBeReversed.charAt(i - 1);                 //printing the character at index i-1
//      i--;
//      //decreasing the length of the string
//    } toBeReversed = toBeReversed;};
//String b;
    for (int i = toBeReversed.length(); i >= 0; i--) {
      ArrayList
          b = toBeReversed.charAt() ;
    }
      System.out.println(b);

  }
}
