import java.util.ArrayList;
import java.util.Scanner;

public class Reverse {
  public static void main(String[] args) {

    // Create a method that can reverse a String, which is passed as the parameter
    // Use it on this reversed string to check it!
    // Try to solve this using charAt() first, and optionally anything else afte//    int i = rfrbgrgrgrrgrgr.length();                   //determining the length of the string/    while (i > 0) {{

//    Scanner

    String reversedOne = "Innen írom tovább: ";
    String rfrbgrgrgrrgrgr = ".eslaf eb t'ndluow ecnetnes siht ,dehctiws erew eslaf dna eurt fo sgninaem eht fI";
    reverseOption0(1argument, 2argument);
  }

  private static void reverseOption(String 1argument, String 2argument) {
    for (int i = toBeReversed.length()-1; i >= 0; i--) {
      reversed1 += toBeReversed.charAt(i);
    }
    System.out.print(reversed1);
  }
}